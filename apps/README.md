# apps/

Deployable applications live here, one directory per app (`apps/web`, `apps/api`, `apps/cli`, ...). Even a single app gets its own directory: the fixed layout is what keeps every repo built from this template navigable to agents.

Inside an app, group feature code by domain (vertical slices), not by technology. `/setup-stack` scaffolds the first app.
